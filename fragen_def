🔹 Регрессия (предсказание числового показателя)

Предсказать VO2max — показатель аэробной выносливости по возрасту, полу, весу, тренировкам, сну и т. д.
→ Целевая переменная: vo2max
→ Полезно для оценки физической формы без проведения теста.

Предсказать уровень жира в теле (body_fat_pct) по антропометрии и образу жизни.
→ Цель: понять, какие привычки сильнее влияют на процент жира.

Предсказать время на 5 км (run_5k_min) по фитнес-показателям и тренировкам.
→ Цель: построить модель прогноза спортивных результатов.

............

Для регрессии:

Linear Regression – базовая линейная модель, интерпретируема.

Random Forest Regressor – нелинейная, устойчиво работает с разными типами данных.

XGBoost – более мощная бустинговая модель, часто даёт лучшие результаты.